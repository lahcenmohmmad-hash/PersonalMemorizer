<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#F0F0F0">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Personal Memorizer"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="30dp"
        android:textColor="#333333"/>

    <Button
        android:id="@+id/btnFixBattery"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="1. Fix Battery (One Time)"
        android:layout_marginBottom="10dp"/>

    <!-- زر الملف الأول -->
    <Button
        android:id="@+id/btnSelectAudio1"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="2. Select File A (Main)"
        android:layout_marginBottom="10dp"/>

    <!-- زر الملف الثاني (الجديد) -->
    <Button
        android:id="@+id/btnSelectAudio2"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="3. Select File B (Optional)"
        android:backgroundTint="#2196F3"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="20dp"/>

    <Button
        android:id="@+id/btnStart"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="4. Start Memorizing"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="10dp"/>

    <Button
        android:id="@+id/btnStop"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Stop Session"
        android:backgroundTint="#F44336"
        android:textColor="#FFFFFF"/>

</LinearLayout>
